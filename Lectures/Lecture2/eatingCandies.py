testCases = int(input())

total = 0

while testCases > 0:
  n = int(input())
  weights = list(map(int, input().split()))
  
  aWeight, bWeight = 0, 0
  l, r = 0, n - 1
  
  while l < r:
    aWeight += weights[l]
    bWeight += weights[r]
    
    if aWeight == bWeight:
      total +
      
  
  
  
  testCases -= 1
  
  
  
print